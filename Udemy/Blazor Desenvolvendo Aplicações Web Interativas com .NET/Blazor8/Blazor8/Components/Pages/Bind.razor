@page "/bind"
@rendermode InteractiveServer
@*
    rendermode:
        (ServerPrerendered: renderiza no servidor e envia o html para o cliente
        Server: renderiza no servidor e envia o html para o cliente
        WebAssembly: renderiza no cliente)
        - None (Sem interatividade): Vazio
        - Server: InteractiveServer
        - WebAssembly: InteractiveWebAssembly
        - Auto (Server > WebAssembly): InteractiveAuto
*@
<h3>Bind</h3>

<div>
    @*
        Bind | One-Way (Unidirectional): get
    *@
    <p>@Texto</p>
</div>

<div>
    @*
        Bind | Two-Way (Bidirectional): get e set
    *@
    <input @bind="Texto" class="form-control" />
</div>

<div class="mt-3">
    <input @bind="Texto" @bind:event="oninput" class="form-control" />
</div>

@code {
    private string Texto { get; set; } = "Eu sou um texto";
}