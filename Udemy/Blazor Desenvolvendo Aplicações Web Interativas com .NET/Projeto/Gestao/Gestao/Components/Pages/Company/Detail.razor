@page "/Company/Detail/{id:int}"

@rendermode InteractiveServer
@inject IJSRuntime JS
@inject NavigationManager NavigationManager
@inject AuthenticationStateProvider AuthenticationStateProvider
@inject ICompanyRepository CompanyRepository


@* <PageTitle>Empresa Detalhe</PageTitle>

<div class="container-fluid py-2">
    <div class="row">
        <div class="col-lg-12">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-white border-bottom-0 d-flex align-items-center justify-content-between">
                    <h4 class="card-title mb-0 text-primary fw-bold">Detalhes da Empresa</h4>
                </div>
                <div class="card-body">
                    <div class=" row g-3">
                        <div class="col-xxl-3 col-md-12">
                            <label for="razaosocial" class="form-label fw-semibold">Razão Social: </label>
                            @Company!.LegalName
                        </div>
                        <div class="col-xxl-3 col-md-12">
                            <label for="nomefantasia" class="form-label fw-semibold">Nome Fantasia: </label>
                            @Company!.TradeName
                        </div>
                        <div class="col-xxl-3 col-md-12">
                            <label for="cnpj" class="form-label fw-semibold">CNPJ: </label>
                            @Company!.TaxId
                        </div>
                    </div>
                    <div class=" row g-3 mt-2">

                        <div class="col-xxl-3 col-md-12">
                            <label for="cep" class="form-label fw-semibold">CEP: </label>
                            @Company!.PostalCode
                        </div>
                        <div class="col-xxl-3 col-md-6">
                            <label for="cidade" class="form-label fw-semibold">Cidade: </label>
                            @Company!.City
                        </div>
                        <div class="col-xxl-3 col-md-6">
                            <label for="estado" class="form-label fw-semibold">Estado: </label>
                            @Company!.State
                        </div>
                    </div>
                    <div class=" row g-3 mt-2">
                        <div class="col-xxl-6 col-md-6">
                            <label for="endereco" class="form-label fw-semibold">Endereço: </label>
                            @Company!.Address
                        </div>
                        <div class="col-xxl-3 col-md-6">
                            <label for="bairro" class="form-label fw-semibold">Bairro: </label>
                            @Company!.Neighborhood
                        </div>
                        <div class="col-xxl-3 col-md-6">
                            <label for="complemento" class="form-label fw-semibold">Complemento: </label>
                            @Company!.Complement
                        </div>
                    </div>

                    <hr class="my-4">

                    <div class="d-flex justify-content-between">
                        <a href="/Company/List" class="btn btn-outline-secondary px-4">Voltar</a>
                        <button type="submit" class="btn btn-danger px-4">Excluir</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> *@

<PageTitle>Empresa - Confirmar Exclusão</PageTitle>

<div class="container-fluid py-2">
    <div class="row ">
        <div class="col-lg-12">
            <div class="card shadow-sm border-0 ">
                <div class="card-header bg-danger border-bottom-0 d-flex align-items-center justify-content-between">
                    <h4 class="card-title mb-0 text-white">Confirmar exclusão da empresa</h4>
                </div>

                <div class="card-body p-4">
                    <div class="alert alert-warning border-warning d-flex align-items-center" role="alert">
                        <i class="bi bi-exclamation-circle-fill me-2 fs-5"></i>
                        <div>
                            Tem certeza de que deseja <strong>excluir esta empresa</strong>? Esta ação é
                            <strong>irreversível</strong>.
                        </div>
                    </div>

                    <h5 class="fw-bold text-primary mt-4 mb-3">
                        <i class="bi bi-building me-2"></i>Detalhes da Empresa
                    </h5>

                    <div class="row g-3 small text-secondary">
                        <div class="col-md-6 col-lg-4">
                            <label class="fw-semibold text-dark">Razão Social:</label><br />
                            <span>@Company!.LegalName</span>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <label class="fw-semibold text-dark">Nome Fantasia:</label><br />
                            <span>@Company!.TradeName</span>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <label class="fw-semibold text-dark">CNPJ:</label><br />
                            <span>@Company!.TaxId</span>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <label class="fw-semibold text-dark">CEP:</label><br />
                            <span>@Company!.PostalCode</span>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <label class="fw-semibold text-dark">Cidade:</label><br />
                            <span>@Company!.City</span>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <label class="fw-semibold text-dark">Estado:</label><br />
                            <span>@Company!.State</span>
                        </div>
                        <div class="col-md-12 col-lg-8">
                            <label class="fw-semibold text-dark">Endereço:</label><br />
                            <span>@Company!.Address</span>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <label class="fw-semibold text-dark">Bairro:</label><br />
                            <span>@Company!.Neighborhood</span>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <label class="fw-semibold text-dark">Complemento:</label><br />
                            <span>@Company!.Complement</span>
                        </div>
                    </div>

                    <hr class="my-4">

                    <div class="d-flex justify-content-between">
                        <a href="/Company/List" class="btn btn-outline-secondary px-4">Voltar</a>
                        <button @onclick="Remove" type="button" class="btn btn-soft-danger px-4">
                            <i class="ri-delete-bin-line"></i>Excluir Empresa
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@code {
    [Parameter] public int Id { get; set; }
    private Company Company { get; set; } = new();
    private bool IsLoadingAddress { get; set; } = false;

    protected override async Task OnParametersSetAsync()
    {
        var UserId = (await AuthenticationHelper.GetAuthenticationdUserIdAsync(AuthenticationStateProvider)).Value;
        var company = await CompanyRepository.Get(Id);

        if (company is null)
        {
            await JS.InvokeVoidAsync("ShowToast", "alert", "Empresa não encontrada!", "alert", "/Company/List");
        }
        if (company!.UserId != UserId)
        {
            await JS.InvokeVoidAsync("ShowToast",
            "alert", "Você não tem permissão para editar esta empresa!", "info", "/Company/List");
        }
        Company = company;
    }
    private async Task Remove()
    {
        //Salvar
        await CompanyRepository.Delete(Company.Id);

        //Informar e redirecionar
        // Exibir toast e redirecionar depois que o usuário fechar
        await JS.InvokeVoidAsync("ShowToast",
        "Sucesso", "Empresa excluída com sucesso!", "info", "/Company/List");
    }
}